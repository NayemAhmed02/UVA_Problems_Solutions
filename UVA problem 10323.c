

#include<stdio.h>

int main()
{
    long long int input,output,i;

    while(scanf("%lld", &input) != EOF)
    {
        if(input < 0)
        {
            if(input % 2 == 0)
                printf("Underflow!\n");
            else
                printf("Overflow!\n");
        }
        else if(input < 8)
            printf("Underflow!\n");
        else if(input > 13)
            printf("Overflow!\n");
        else
        {
            output = 5040;
            for(i=8; i<=input; i++)
                output = output * i;
            printf("%lld\n",output);
        }
    }
    return 0;
}































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































